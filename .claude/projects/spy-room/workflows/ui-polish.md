# UI Polish Workflow

1. Read `ui-ux.md` for theme and component rules
2. Read the target screen's current code
3. Check similar screens for consistent patterns
4. Make changes using existing `colors` object and `AppButton`
5. Verify dark/light mode both work
6. Consider Android-first — test assumptions against Expo Go
